name: load-metrics

on:
  workflow_dispatch:
    inputs:
      partition:
        description: "Specific date partition to ETL (defaults to latest if blank)."
        required: false

  schedule:
    - cron: "14 7 * * *" # Every day at 7:14 AM UTC

env:
  METRICS_PROD_ENV: "prod"
  # Concurrent GCS blob download threads. The S3 log extract fetches >100k tiny
  # objects per day and is latency-bound. Left at the code default (32); tune
  # here if a run's download phase is the bottleneck.
  GCS_DOWNLOAD_WORKERS: "32"

jobs:
  load-metrics:
    # Larger third-party runner: the S3 log extract is I/O-heavy and the default
    # 4-vCPU runner is the tightest resource. Downloads scale with concurrency,
    # not cores, so 8 vCPUs / 32 GB is plenty.
    runs-on: blacksmith-8vcpu-ubuntu-2404
    # Fast-fail: a healthy run finishes in well under an hour. If it regresses to
    # multi-hour, kill it early instead of burning the full 6-hour default.
    timeout-minutes: 60
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Notify Zulip of workflow launch
        uses: zulip/github-actions-zulip/send-message@v2
        with:
          api-key: ${{ secrets.ZULIP_API_KEY }}
          email: "build-status-bot@catalyst-cooperative.zulipchat.com"
          organization-url: "https://catalyst-cooperative.zulipchat.com"
          type: "stream"
          to: "pudl-deployments"
          topic: "pudl-usage-metrics"
          content: |
            # :rocket: Launching `load-metrics` workflow ([view run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}))
      - uses: actions/checkout@v7
        with:
          fetch-depth: 2

      - name: Install usage metrics environment using pixi
        uses: prefix-dev/setup-pixi@v0.10.2
        with:
          locked: true
          cache: true

      # Authentication via credentials json
      - id: "auth"
        uses: "google-github-actions/auth@v3"
        with:
          workload_identity_provider: "projects/345950277072/locations/global/workloadIdentityPools/gh-actions-pool/providers/gh-actions-provider"
          service_account: "pudl-usage-metrics-etl@catalyst-cooperative-pudl.iam.gserviceaccount.com"
          create_credentials_file: true

      - name: "Set up Cloud SDK"
        uses: "google-github-actions/setup-gcloud@v3"
        with:
          version: ">= 363.0.0"

      - name: Run ETL on the latest day's data
        id: load-data
        env:
          IPINFO_TOKEN: ${{ secrets.IPINFO_TOKEN }}
          PARTITION: ${{ github.event.inputs.partition }}
        shell: bash -l {0}
        run: |
          pixi run usage-metrics etl ${PARTITION:+--partition "$PARTITION"}

      - name: Notify Zulip
        if: always()
        uses: zulip/github-actions-zulip/send-message@v2
        with:
          api-key: ${{ secrets.ZULIP_API_KEY }}
          email: "build-status-bot@catalyst-cooperative.zulipchat.com"
          organization-url: "https://catalyst-cooperative.zulipchat.com"
          type: "stream"
          to: "pudl-deployments"
          topic: "pudl-usage-metrics"
          content: |
            ${{ job.status == 'success' && ':check:' || job.status == 'failure' && ':x:' || ':warning:' }} `load-metrics` completed with status `${{ job.status }}`.
            [View workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
